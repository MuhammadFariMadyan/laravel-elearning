<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Kelas extends Model
{
    protected $table = 'kelas_have_mata_pelajarans';
    // protected $primaryKey = 'id';
     // protected $fillable = ['nama_kelas', 'id_mapel', 'created_at', 'updated_at'];
}
